<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock" x:Class="SemanticRelationsResolver.Annotator.View.MainWindow"
        Title="Annotator" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Menu x:Name="MainMenu" IsMainMenu="True">
            <MenuItem x:Name="FileMenu" Header="_FILE">
                <MenuItem x:Name="New" Header="New">
                    <MenuItem x:Name="Treebank" Header="_Treebank" Click="TreebankClick"></MenuItem>
                    <MenuItem x:Name="TreebankFromExisting" Header="_Treebank from existing" Click="TreebankFromExistingClick"></MenuItem>
                </MenuItem>
                <MenuItem x:Name="Open" Header="_Open" Click="OpenClick"></MenuItem>
                <Separator/>
                <MenuItem x:Name="Save" Header="Save" Click="SaveClick"></MenuItem>
                <MenuItem x:Name="SaveAs" Header="Save As" Click="SaveAsClick"></MenuItem>
                <Separator/>
                <MenuItem x:Name="Exit" Header="_Exit" Click="ExitClick"></MenuItem>
            </MenuItem>
            <MenuItem x:Name="EditMenu" Header="_EDIT">
                <MenuItem Header="_Undo"></MenuItem>
                <MenuItem Header="_Redo"></MenuItem>
                <Separator/>
                <MenuItem Header="_Cut"></MenuItem>
                <MenuItem Header="_Copy"></MenuItem>
                <MenuItem Header="_Paste"></MenuItem>
                <MenuItem Header="_Delete"></MenuItem>
            </MenuItem>
            <MenuItem x:Name="ViewMenu" Header="_VIEW">
                <MenuItem Header="_Tree Explorer"></MenuItem>
                <MenuItem Header="_Sentence List"></MenuItem>
                <Separator/>
                <MenuItem Header="_Sentence Graph"></MenuItem>
            </MenuItem>
            <MenuItem x:Name="HelpMenu" Header="_HELP" />
        </Menu>

        <StatusBar x:Name="StatusBar" Grid.Row="2" Focusable="False" IsHitTestVisible="False" />
        
        <xcad:DockingManager Grid.Row="1">
            
        </xcad:DockingManager>
    </Grid>
</Window>
